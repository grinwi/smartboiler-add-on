flask>=3.0.0
influxdb>=5.3
numpy>=1.24
pandas>=2.0
pytz
requests>=2.31
# Installed from the tagged git release — no PyPI propagation delay.
# update-and-build.yml updates the @v<tag> on each library release.
smartboiler @ git+https://github.com/grinwi/smartboiler.git@v1.5.1
